import { type TAccountFeatureViewer } from '@/lib/account/server/initialData';

interface IProps {
	viewer?: TAccountFeatureViewer | null;
}

export default function AnnouncementBar(props: IProps) {
	void props;
	return null;
}
