---
name: 账号系统落地总纲
overview: 基于最终方案拆解账号系统接入现有项目的实施顺序、文档索引、状态和风险。
isProject: false
---

# 账号系统落地总纲

> 日期：2026-05-17
> 基准方案：[账号系统方案与接入报告.plan.md](账号系统方案与接入报告.plan.md)

## 一、落地目标

- 在自托管且 SQLite 可写环境启用站内账号密码、管理员、账号同步和旧备份码导入。
- 在 Vercel、`OFFLINE=true`、静态导出、无持久化 serverless 环境保持现有本地流程，不加载账号服务端模块。
- 登录前继续使用现有本地存储、本地导入导出和页面交互。
- 登录或注册后，把当前设备已有的可同步数据接入账号系统，并自动增量同步。
- 旧备份码只作为导入入口，导入成功后立即失效。

## 二、实施文档

| 文档                                                                                 | 交付物                                                  | 完成判定                                           |
| ------------------------------------------------------------------------------------ | ------------------------------------------------------- | -------------------------------------------------- |
| [01-服务端与数据库落地.plan.md](01-服务端与数据库落地.plan.md)                       | 运行门禁、DB schema、Kysely 类型、迁移、CORS            | 禁用环境不加载账号 DB，启用环境迁移幂等            |
| [02-认证会话与管理员落地.plan.md](02-认证会话与管理员落地.plan.md)                   | 普通认证、session、CSRF、管理员环境变量账号             | 普通账号和管理员接口闭环，session 写接口 CSRF 生效 |
| [03-同步协议与状态引擎落地.plan.md](03-同步协议与状态引擎落地.plan.md)               | `user_state` 协议、dirty queue、lease、sendBeacon、冲突 | 多 namespace 部分成功、冲突和重试契约稳定          |
| [04-客户端接入与引导落地.plan.md](04-客户端接入与引导落地.plan.md)                   | 导航栏账号入口、显式账号窗口、登录后本地数据接管        | 未登录流程不变，点击账号按钮后可登录或注册         |
| [05-namespace序列化与冲突合并落地.plan.md](05-namespace序列化与冲突合并落地.plan.md) | 字段白名单、schema version、迁移、合并规则              | 每个 namespace 均有 serializer、validator、merge   |
| [06-设置页与旧备份码迁移落地.plan.md](06-设置页与旧备份码迁移落地.plan.md)           | 设置页改造、旧备份码导入、匿名云备份入口下线            | 旧备份码一次性导入并按 namespace 合并              |
| [07-验证清单与发布回归.plan.md](07-验证清单与发布回归.plan.md)                       | 单测、手测、API、禁用环境、多标签页、多设备回归         | 发布前命令和人工场景通过                           |

## 三、实施顺序

| 阶段 | 修改范围                                             | 完成判定                                                         |
| ---- | ---------------------------------------------------- | ---------------------------------------------------------------- |
| 0    | 运行门禁、依赖、CORS 排除、DB 动态导入               | 禁用环境账号 API 返回 `feature-disabled`，构建不触发 SQLite      |
| 1    | SQLite schema、Kysely 类型、账号 actions             | 重复启动迁移幂等，旧 `backup_files` 不受影响                     |
| 2    | 普通认证、管理员认证、session、CSRF                  | 注册登录改密闭环，管理员不写入普通用户表                         |
| 3    | 同步 API、`state_epoch`、逐 namespace 响应           | 多 namespace PUT 成功项和冲突项可分开处理                        |
| 4    | namespace serializer、validator、merge               | 只上传白名单字段，不上传完整 store `persistence`                 |
| 5    | 客户端账号 store、导航栏账号入口、登录后本地接管     | 未登录不影响现有流程，登录后本地数据自动接管                     |
| 6    | dirty queue、BroadcastChannel、lease、sendBeacon     | 多标签页只有一个 uploader，断网和关闭页都有兜底                  |
| 7    | 设置页数据管理改造、旧备份码导入、匿名云备份入口下线 | 账号功能启用时数据管理展示旧备份码导入，导入成功后本地 code 清除 |
| 8    | 验证、回归、发布前检查                               | `pnpm exec prettier --check`、`pnpm lint`、关键手测通过          |

## 四、当前代码落地状态

| 阶段 | 状态   | 已落地范围                                                                                                                                                     |
| ---- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0    | 已完成 | 精确环境门禁、Argon2 依赖、DB 动态导入、legacy CORS 白名单、SW API 绕过                                                                                        |
| 1    | 已完成 | 账号 SQLite schema、幂等迁移、缺列补齐、Kysely 类型、account actions                                                                                           |
| 2    | 已完成 | 普通认证、普通 session、CSRF、管理员环境变量账号、管理员用户管理 API                                                                                           |
| 3    | 已完成 | `GET/PUT /api/v1/sync/state`、`POST /api/v1/sync/ping`、逐 namespace 响应                                                                                      |
| 4    | 已完成 | namespace serializer、validator、migration、merge                                                                                                              |
| 5    | 已完成 | 客户端账号 store、导航栏显式账号窗口、登录注册、登录后本地接管                                                                                                 |
| 6    | 已完成 | dirty queue、BroadcastChannel、lease、sendBeacon 客户端调度                                                                                                    |
| 7    | 已完成 | 旧备份码导入 API、设置页数据管理导入入口、导航栏账号窗口、管理员页面、同步状态、旧匿名云备份入口下线                                                           |
| 8    | 进行中 | `pnpm exec tsc --noEmit`、`pnpm lint`、`pnpm build` 通过，仅保留既有 Sass/`onClick` deprecated 警告；仍需补跑 prettier、禁用/自托管/offline 环境矩阵和关键手测 |

## 五、复用现有能力

- 账号 API 响应复用 [app/api/v1/utils.ts](../../../app/api/v1/utils.ts) 的 `createNoStoreJsonResponse`、`createNoStoreErrorResponse`、`handleOptionsRequest`。
- DB 复用 [app/lib/db/db.ts](../../../app/lib/db/db.ts) 的 Kysely + `better-sqlite3`，并扩展 [app/lib/db/constant.ts](../../../app/lib/db/constant.ts) 与 [app/lib/db/types.d.ts](../../../app/lib/db/types.d.ts)。
- 旧备份码导入复用 [app/actions/backup](../../../app/actions/backup) 的 DB、文件和兼容函数。
- 本地持久化复用 [app/stores/middlewares/persist.ts](../../../app/stores/middlewares/persist.ts) 的压缩存储思路和 [app/utilities](../../../app/utilities) 的 `safeStorage`。
- 多标签页同步复用 [app/stores/middlewares/sync.ts](../../../app/stores/middlewares/sync.ts) 的 BroadcastChannel 思路，但账号云同步使用独立 channel。
- 设置页接入基于 [app/(pages)/preferences/dataManager.tsx](<../../../app/(pages)/preferences/dataManager.tsx>) 保留数据管理；账号 UI 拆成独立组件后由导航栏账号窗口承载。

## 六、代码风格约束

- 类型命名沿用 `I*`、`T*`：`IAuthLoginBody`、`IAccountMeSuccessResponse`、`ISyncStateChange`、`TSyncNamespace`。
- DB 类型沿用 `ITable*`、`T*New`、`T*Update`：`ITableUser`、`TUserNew`、`TUserUpdate`。
- 常量使用 `*_MAP`：`USER_STATUS_MAP`、`ACCOUNT_COOKIE_NAME_MAP`、`SYNC_NAMESPACE_MAP`。
- 逻辑优先写成纯函数：serializer、validator、migration、merge 都不依赖 React 状态。
- 集合转换优先使用 `map`、`forEach`、`reduce`、`filter`，避免类式状态机。
- API route 使用 App Router 现有写法：`export async function POST(request: NextRequest)`。
- 修改保持局部化；客户端 UI 接入依赖服务端认证与同步 API 通过验证清单。

## 七、当前风险索引

| 风险                                                                            | 处理文档                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/api/v1/:path*` 现有 wildcard CORS 与账号 Cookie/CSRF 冲突                     | [01-服务端与数据库落地.plan.md](01-服务端与数据库落地.plan.md)                                                                                                                                                               |
| 禁用环境静态导入 SQLite 或 Argon2 造成构建/运行失败                             | [01-服务端与数据库落地.plan.md](01-服务端与数据库落地.plan.md)                                                                                                                                                               |
| 管理员重置密码后普通用户永不过期 session 继续有效                               | [02-认证会话与管理员落地.plan.md](02-认证会话与管理员落地.plan.md)                                                                                                                                                           |
| 远端状态写回本地时再次标记 dirty                                                | [03-同步协议与状态引擎落地.plan.md](03-同步协议与状态引擎落地.plan.md)                                                                                                                                                       |
| 直接上传完整 `persistence` 导致 `userId/cloudCode` 等设备或 legacy 字段泄入云端 | 已通过阶段 4 serializer 白名单缓解，`donationModal` 仅同步账号级触发状态且不包含 `shared.donationModal.isOpen`；仍需阶段 8 回归验证；见 [05-namespace序列化与冲突合并落地.plan.md](05-namespace序列化与冲突合并落地.plan.md) |
| 旧备份码并发导入或文件删除失败导致 code 复活                                    | [06-设置页与旧备份码迁移落地.plan.md](06-设置页与旧备份码迁移落地.plan.md)                                                                                                                                                   |
