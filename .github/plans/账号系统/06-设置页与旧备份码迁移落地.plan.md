---
name: 设置页与旧备份码迁移落地
overview: 设置页数据管理、旧备份码导入和匿名云备份入口下线步骤。
isProject: false
---

# 设置页与旧备份码迁移落地

## 一、目标

- 设置页保留本地导入导出。
- 未登录用户在数据管理中看到旧备份码导入说明和登录/注册入口，不录入或暂存新备份码。
- 账号功能启用后，登录用户使用账号同步，不展示匿名云备份上传、还原、删除主入口。
- 账号登录、注册、同步状态和账号操作从设置页独立到导航栏账号窗口中。
- 账号功能禁用时，设置页保持当前环境原有可用能力；offline/static 只展示本地导入导出，legacy server 环境保留旧匿名云备份入口。
- 旧备份码导入成功后清除本地 `cloudCode`。

## 二、现有代码

- [app/(pages)/preferences/dataManager.tsx](<../../../app/(pages)/preferences/dataManager.tsx>) 当前包含本地导入导出、匿名云备份上传、还原、删除。
- [app/api/v1/backups/route.ts](../../../app/api/v1/backups/route.ts) 是旧匿名云备份上传。
- [app/api/v1/backups/[code]/route.ts](../../../app/api/v1/backups/[code]/route.ts) 是旧备份码下载和删除。
- [app/api/v1/backups/[code]/metadata/route.ts](../../../app/api/v1/backups/[code]/metadata/route.ts) 是旧备份码元数据查询。
- [app/api/v1/backups/cleanup/route.ts](../../../app/api/v1/backups/cleanup/route.ts) 是旧备份清理入口，secret 从 `x-cleanup-secret` header 读取。
- [app/actions/backup/compatibility.ts](../../../app/actions/backup/compatibility.ts) 兼容旧稀客-only 数据。
- [app/actions/backup/db.ts](../../../app/actions/backup/db.ts) 和 [app/actions/backup/file.ts](../../../app/actions/backup/file.ts) 可复用到导入。

## 三、新增文件

| 文件                                                         | 作用                                           |
| ------------------------------------------------------------ | ---------------------------------------------- |
| `app/lib/account/client/components/accountManager.tsx`       | 账号窗口中的登录状态、登录/注册/退出、账号操作 |
| `app/lib/account/client/components/accountSyncStatus.tsx`    | 最近同步、待上传、失败、冲突入口               |
| `app/lib/account/client/components/legacyBackupImport.tsx`   | 输入旧备份码并导入到账号                       |
| `app/lib/account/client/components/accountConflictModal.tsx` | 全局冲突选择窗口，设置页只触发打开             |
| `app/api/v1/sync/import-backup-code/route.ts`                | 旧备份码一次性导入兼容 API                     |
| `app/lib/account/client/api.ts`                              | 站内旧备份码导入 API 客户端                    |

## 四、设置页状态

| 状态           | 展示                                                                     |
| -------------- | ------------------------------------------------------------------------ |
| 账号功能禁用   | 保持当前环境原有可用能力；offline/static 只展示本地导入导出              |
| 未登录         | 本地导入导出、旧备份码导入说明和登录/注册入口；不展示备份码输入框        |
| 已登录         | 本地导入导出、旧备份码导入入口；账号同步状态和账号操作通过导航栏账号窗口 |
| 已登录且有失败 | 数据管理不展示同步失败状态；导航栏账号窗口展示失败和重试入口             |
| 已登录且有冲突 | 数据管理不展示冲突入口；全局冲突窗口处理当前账号冲突                     |

## 五、旧备份码导入 API

`POST /api/v1/sync/import-backup-code` 步骤：

1. 校验普通 session。
2. 对 code 执行 `trim()` 后校验 UUID 格式，客户端自动导入和服务端 API 使用同一个规范化 code。
3. 使用规范化 code 定位旧备份；legacy 上传和账号导入同一 code 时通过进程内 code lock 串行执行。
4. 根据 `backup_files.file_name` 定位 legacy JSON；旧记录 `file_name` 为空时回退读取 `upload/backups/{code}.json`。
5. 读取前检查备份 JSON 文件大小，沿用 legacy 匿名上传 10MB UTF-8 byte 上限；超过上限返回稳定 `invalid-backup-file`。
6. 兼容旧稀客-only 数据和 `{ customer_normal, customer_rare }` 数据。
7. 对导入套餐先执行 `deleteIndexProperty` 清洗，再执行旧稀客兼容、validate、按 namespace 白名单规范化、merge。
8. 旧稀客-only 数据映射到 `customer_rare.meals`。
9. `{ customer_normal, customer_rare }` 分别映射到 `customer_normal.meals` 和 `customer_rare.meals`。
10. 账号已有云端套餐时，使用 [05-namespace序列化与冲突合并落地.plan.md](05-namespace序列化与冲突合并落地.plan.md) 的套餐合并规则，不覆盖整份云端数据。
11. 每个被写入 namespace 的 revision 加 1。
12. 旧备份码导入不递增 `state_epoch`。
13. 在 SQLite transaction 中先校验当前 `users.state_epoch`；不匹配时返回 `state-epoch-mismatch`，不消费 code。
14. 在 fresh code lock 下的短 SQLite transaction 中按预检到的 `file_name` 条件删除 `backup_files` 记录；claim 失败且当前 code 仍有记录时返回 `backup-code-lock-lost`，不写入 `user_state`。
15. claim 成功后按当前 namespace revision 执行条件 insert/update；并发普通同步已推进 revision 时回滚并返回 `sync-conflict`，不消费 code。
16. transaction 成功并写入 `backup_imports.file_name` 后，服务端在锁外最佳努力删除本次 claim 的 JSON 文件；同用户同 code 同 `state_epoch` 幂等重试时会复用记录的 `file_name` 再次尝试删除。
17. cleanup 会扫描无 DB record 指向的孤儿 JSON 文件并尝试清理；删除前会获取同 code 锁并重新确认 DB 指针，作为导入删除失败、过期记录和孤儿文件的兜底。
18. 返回每个被导入 namespace 的 `namespace`、`revision`、`status`。
19. 客户端收到成功响应后通过 `client/api.ts` 读取 sync state 校准 revision，再执行本地数据接管。

## 六、旧入口下线

- 未登录用户不再展示“备份至云端”。
- 未登录用户不录入或保存新的旧备份码；旧备份码导入 tab 提示先通过导航栏账号窗口登录或注册。
- 登录用户不再展示匿名云备份上传、还原、删除按钮。
- `globalStore.persistence.cloudCode` 只用于显示旧备份码导入提示。
- 导入成功后执行 `globalStore.persistence.cloudCode.set(null)`。
- 导入失败时保留本地 `cloudCode`，设置页继续展示重试入口。

## 七、本地导入导出保留

- 本地导出继续使用 `downloadJson`。
- 本地导入继续使用 `parseJsonFromInput`。
- 本地导出包含 `customer_normal_meals`、`customer_rare_meals` 和 `customer_rare_plans`，分别对应普客套餐、稀客套餐和稀客营业预设。
- 本地导入按字段恢复：文件中存在的类别完整覆盖对应本地数据，缺少的类别保持本地现状；继续兼容 `{ customer_normal, customer_rare }` 和更早的稀客-only 文件。同一文件混用新旧类别 key 时拒绝导入。
- 本地导入在修改 store 前完成所有已包含类别的校验，任一类别无效时不写入任何本地数据。
- 本地导入后，如果用户已登录，则导入结果进入 dirty queue，由账号同步上传。
- 顾客套餐和营业预设分别提供本地重置入口；如果用户已登录，则对应空快照进入 dirty queue，由账号同步上传。

## 七点一、账号入口复用与账号导出下线

> 状态：已实施，待完成发布回归。

- 将本地导入/导出内容提取为独立客户端组件，设置页数据管理的第一个 tab 与账号子弹窗复用同一实现，不复制导入校验、下载或 store 写入逻辑。
- 设置页数据管理保持完整功能和现有全局搜索入口，继续包含本地导入/导出、旧备份码导入、兼容环境下的匿名云备份和重置功能。
- 登录后的账号面板将“导出账号数据”按钮替换为“数据管理”；点击后使用覆盖层协调器从 `account.main` 打开 `account.data-manager` 子 Modal，直接展示本地导入/导出内容，不显示 tabs、旧备份码、匿名云备份、重置或教程入口。
- `account.data-manager` 与 `account.legal` 使用相同的账号子任务生命周期：父账号窗口保持 covered，关闭子 Modal 后恢复账号窗口并把焦点交还给合理的父级触发元素。
- 删除 `/api/v1/account/export` route、`exportAccountData` 客户端方法、`IAccountExportData` 共享类型、账号窗口导出处理函数、导出成功消息和 `accountDataExported` 当前审计动作。旧地址升级后返回 404，不保留隐藏入口或兼容 API。
- 管理端继续保留 `user-export-account-data` 的中文 action 映射，仅用于展示升级前已存在的历史审计记录，不表示当前仍可执行账号导出。
- 法律声明没有提供账号数据导出、下载副本或数据可移植能力的承诺；“账号注销与数据留存”只约定注销后的删除、去标识化和法定日志留存，因此下线账号导出不需要修改法律声明。
- 全局搜索现有“数据管理”结果和打开设置页数据管理区域的行为保持不变；“账号同步”结果仍打开账号窗口，只删除“导出账号数据”相关的过时描述和关键词。
- 账号危险操作说明改为“危险操作会影响云端数据或账号本身，请先通过数据管理导出需要保留的数据。”，与同一区域上方“数据管理”按钮形成对应。
- 子弹窗中的导入仍只写本地 store，账号登录、正常同步、云同步暂停和冲突处理继续使用现有 watcher、dirty queue 与冲突流程；本次不新增账号级导入协议。

## 八、验证点

- 未登录仍可本地导入导出。
- 新导出的本地文件包含普客套餐、稀客套餐和稀客营业预设；导入仅套餐、仅营业预设或旧稀客-only 文件时，文件未包含的本地类别保持不变。
- 本地导入文件任一已包含类别校验失败时，普客套餐、稀客套餐和营业预设均保持导入前状态。
- 顾客套餐和营业预设可以分别重置；正常云同步时对应空快照上传，云同步暂停时只保存在本地待用户主动恢复同步。
- 登录用户看不到匿名云备份上传、还原、删除主入口。
- 本地已有 `cloudCode` 时登录或注册后自动导入；未登录旧备份码 tab 只提示先登录或注册，不保存新输入的 code。
- 同一个 code 并发导入只有一个成功。
- code 前后带空白时，已登录手动导入和已有本地 `cloudCode` 的登录后自动导入都按 trim 后 UUID 处理。
- 已登录用户可逐个导入多个不同旧备份码；每个 code 成功后立即从 legacy 备份表消费，导入数据按套餐签名合并到当前账号，已有相同套餐跳过。
- 同一 code 对同用户同 `state_epoch` 的重试为幂等返回，不能被其他用户或其它账号状态再次消费。
- legacy CORS 回归只覆盖 backups 根路由、`[code]` 和 `metadata`；cleanup 路由不开放 wildcard CORS，secret 通过 `x-cleanup-secret` header 传入。
- 并发导入同一个 code 时，失败请求不会写入 `user_state`。
- 旧备份上传和账号导入都按 UTF-8 byte 计算 10MB 上限。
- cleanup 返回删除/失败计数而不返回完整 code 列表；未下载过的备份按 `created_at` 判断过期，已下载过的备份按 `last_accessed` 判断过期。过期 DB 记录即使文件已缺失也会清理记录，并会清理 `upload/backups/*.json` 中已无 DB 记录的孤儿文件；cleanup 删除前会获取同 code 锁并重新确认 DB 状态，避免和正在进行的同 code 上传/导入交叉。
- legacy 覆盖上传已有 code 时会重置 `last_accessed = -1`，让新内容按新的 `created_at` 重新计算过期时间。
- 设置页和账号子弹窗复用同一本地导入/导出实现；两处导出格式、导入校验、局部覆盖语义和同步后续行为一致。
- 账号子弹窗不展示数据管理 tabs、旧备份码、匿名云备份、重置或教程入口；关闭后账号窗口恢复，键盘焦点和滚动不进入被覆盖父层。
- 全局搜索“数据管理”仍打开设置页完整数据管理区域；“账号同步”仍打开账号窗口。
- `/api/v1/account/export`、客户端导出调用和新导出审计动作均不存在；历史 `user-export-account-data` 审计记录仍显示中文标签。
- static/offline 构建不因复用本地数据管理组件引入账号 Modal、账号 API 或服务端模块。

## 九、代码落地状态

- 已新增并保留 [app/api/v1/sync/import-backup-code/route.ts](../../../app/api/v1/sync/import-backup-code/route.ts) 作为站内与直接调用方共用的导入 API；设置页和登录后自动导入由 [app/lib/account/client/api.ts](../../../app/lib/account/client/api.ts) 请求该 route，并复用同一服务端领域逻辑。
- 导入 API 已实现普通 session、same-origin、CSRF、trim 后 UUID code 校验、旧稀客-only 兼容、`deleteIndexProperty` 清洗、同 code 进程内串行锁、transaction 内 `state_epoch` 校验、`DELETE FROM backup_files ... RETURNING` claim、旧文件读取与校验、revision 条件写入 `user_state`；成功后记录 `backup_imports.file_name` 并最佳努力删除本次 claim 的 JSON 文件，cleanup 继续兜底回收过期记录和孤儿文件。
- 已新增 [app/lib/account/client/components/accountManager.tsx](../../../app/lib/account/client/components/accountManager.tsx)、[app/lib/account/client/components/accountSyncStatus.tsx](../../../app/lib/account/client/components/accountSyncStatus.tsx) 和 [app/lib/account/client/components/legacyBackupImport.tsx](../../../app/lib/account/client/components/legacyBackupImport.tsx)；账号管理内容由导航栏账号窗口承载，旧备份码导入由设置页数据管理承载。
- 登录、注册或已有 session bootstrap 后会先尝试导入本地 `cloudCode`，成功后清空本地旧备份码；导入失败时保留本地旧备份码并继续执行本地数据接管。未登录时设置页数据管理不录入新备份码，只提供先登录或注册入口；已登录后可在数据管理中手动输入 code 并立即导入，多个不同 code 可逐个导入。
- 登录、注册、已有 session bootstrap 或设置页手动导入期间暂停 watcher 产生的 dirty 入队；导入和本地接管所需的显式 dirty entry 正常写入，流程完成后恢复同步调度。
- 已更新 [app/(pages)/preferences/content.tsx](<../../../app/(pages)/preferences/content.tsx>)，设置页不再接入账号管理区域。
- 已更新 [app/(pages)/preferences/dataManager.tsx](<../../../app/(pages)/preferences/dataManager.tsx>)，账号功能启用时隐藏旧匿名云备份主入口并展示旧备份码导入；账号功能禁用且仍为 self-hosted server 环境时保留 legacy 云备份入口，offline、Vercel/static export 通过 [app/lib/account/client/components/legacyBackupImport.offline.tsx](../../../app/lib/account/client/components/legacyBackupImport.offline.tsx) 排除账号导入 UI，只展示本地导入导出。
- 本地导入导出已覆盖普客套餐、稀客套餐和稀客营业预设，采用“文件中存在的类别覆盖、缺少的类别保留”语义；套餐与营业预设提供独立重置入口，并继续通过现有 store watcher 进入账号同步。
- 已新增 [app/(pages)/preferences/localDataManager.tsx](<../../../app/(pages)/preferences/localDataManager.tsx>)，设置页首个 tab 与账号 `account.data-manager` 子 Modal 复用同一本地导入/导出实现；设置页继续使用默认半宽布局，账号子 Modal 通过 `isFullWidth` 使用完整内容宽度；账号面板入口已改为“数据管理”。
- 已移除 `/api/v1/account/export`、客户端账号导出方法、共享导出类型和当前导出审计动作；管理端仅保留升级前 `user-export-account-data` 历史记录的中文显示。

## 十、账号数据管理入口实施计划

### 文件职责

- 新增 `app/(pages)/preferences/localDataManager.tsx`：只负责本地 JSON 导入、校验、应用和导出 UI，不读取旧备份码、账号会话、重置或教程状态；默认保持设置页半宽布局，账号子 Modal 可显式使用全宽布局。
- 新增 `app/(pages)/preferences/dataManagerScroll.ts`：复用数据管理 Modal 滚动容器定位逻辑。
- 修改 `app/(pages)/preferences/dataManager.tsx`：保留完整 tabs、旧备份和重置功能，第一个 tab 渲染 `LocalDataManager`。
- 修改 `app/lib/account/client/components/accountManager.tsx`：删除账号导出处理，新增“数据管理”按钮和 `account.data-manager` 子 Modal。
- 修改 `app/lib/overlayCoordinator/types.ts` 与 `constants.ts`：登记新的账号子任务 ID。
- 删除 `app/api/v1/account/export/route.ts`，并从客户端 API、共享类型与当前审计动作中移除账号导出契约；`app/(pages)/admin/audit/client.tsx` 的历史 action 中文映射保留。
- 修改 `app/lib/globalSearch/preferences.ts`：完整“数据管理”搜索结果不变，只删除“账号同步”条目里的账号导出文案。
- 修改账号系统 02、06、07、13 计划：移除当前导出 route/验证声明并记录替代入口。

### Task 1：提取可复用的本地数据管理组件

- [x] 创建临时 `local-data-manager-contract.tmp.tsx`，写入以下编译契约：

```tsx
import LocalDataManager from './app/(pages)/preferences/localDataManager';

void LocalDataManager;
```

- [x] 运行 `pnpm exec tsc --noEmit --pretty false --incremental false`，确认因 `localDataManager` 模块尚不存在而失败。
- [x] 新增 `localDataManager.tsx`，从 `dataManager.tsx` 原样迁移本地导入/导出所需的键名、校验、文件读取、确认 Popover、Snippet、下载状态和 Modal 滚动锁；默认导出：

```tsx
export default memo(function LocalDataManager({ isFullWidth = false }: IProps) {
	return (
		<div className={cn('w-full space-y-4', !isFullWidth && 'lg:w-1/2')}>
			{/* 现有第一个 tab 内容 */}
		</div>
	);
});
```

- [x] 修改 `dataManager.tsx`，删除已迁移的本地状态与处理函数，在 `backup-local` tab 中渲染 `<LocalDataManager />`；旧匿名云备份继续只使用 `{ customer_normal, customer_rare }`，重置和教程逻辑不变。
- [x] 重新运行 TypeScript，确认临时契约和项目类型均通过；删除临时契约。

### Task 2：新增账号数据管理子 Modal

- [x] 创建临时 `account-data-manager-overlay-contract.tmp.ts`：

```ts
import type { TOverlayId } from './app/lib/overlayCoordinator/types';

const overlayId: TOverlayId = 'account.data-manager';
void overlayId;
```

- [x] 运行 TypeScript，确认 `'account.data-manager'` 尚未属于 `TOverlayId` 而失败。
- [x] 在 `TOverlayId` 与 `OVERLAY_DEFINITION_MAP` 中登记 `account.data-manager`，使用普通 task 优先级和 `MODAL_DEFAULT_EXIT_DELAY_MS`。
- [x] 在 `accountManager.tsx` 中新增 `isDataManagerModalOpen`，通过以下父子关系打开和关闭：

```ts
pushOverlayChild({
	childId: 'account.data-manager',
	onOpenChild: () => {
		setIsDataManagerModalOpen(true);
	},
	parentId: 'account.main',
});
```

- [x] 将“导出账号数据”按钮替换为带数据类图标的“数据管理”按钮；删除导出 loading、flush、下载和成功消息处理。
- [x] 新增 `account.data-manager` Modal，标题为“数据管理”，渲染 `<LocalDataManager isFullWidth />`；关闭时请求关闭子任务并恢复父账号窗口。
- [x] 更新危险操作说明为“危险操作会影响云端数据或账号本身，请先通过数据管理导出需要保留的数据。”
- [x] 重新运行 TypeScript，确认 overlay 临时契约通过；删除临时契约。

### Task 3：彻底下线账号导出契约

- [x] 删除 `app/api/v1/account/export/route.ts`。
- [x] 从 `app/lib/account/client/api.ts` 删除 `exportAccountData` 和 `IAccountExportData` 的 import/re-export。
- [x] 从 `app/lib/account/shared/types.ts` 删除 `IAccountExportData`。
- [x] 从 `ACCOUNT_AUDIT_ACTION_MAP` 删除 `accountDataExported`；保留管理端 `user-export-account-data: 用户导出账号数据` 映射以显示历史记录。
- [x] 运行以下扫描，预期除管理端历史映射和计划记录外无结果：

```powershell
rg -n "exportAccountData|IAccountExportData|accountDataExported|/api/v1/account/export" app
rg -n "user-export-account-data" app
```

### Task 4：更新搜索与现有计划

- [x] 保持 `data-manager` 搜索项及其设置页打开行为不变。
- [x] 将 `account-sync` 描述改为“查看同步状态，处理同步异常或冲突”，删除“导出账号数据”和“账号数据”关键词，保留同步状态相关关键词。
- [x] 更新 02、07、13 计划中的当前 route、审计和验证声明；06 的七点一状态改为“已实施”，并记录最终文件边界。
- [x] 运行聚焦 Prettier 和 `git diff --check`，确认文档与代码格式通过。

### Task 5：浏览器、运行模式与最终验证

- [x] 使用隔离的开发 SQLite、上传目录、`SELF_HOSTED=true`、本地 `BASE_URL` 和有效 `APP_SECRET` 启动真实浏览器验证。
- [x] 登录账号后确认“数据管理”打开子 Modal；子 Modal 只有本地导入/导出，没有 tabs、旧备份码、重置或教程；关闭后账号窗口恢复。
- [x] 验证 Esc、焦点恢复、父层覆盖、桌面与移动端浅色布局；深色主题和 reduced-motion 本轮未单独回归。
- [x] 从全局搜索打开“数据管理”，确认仍进入设置页完整区域并能看到全部 tabs；“账号同步”仍打开账号窗口。
- [x] 在账号子 Modal 导出并导入一份局部 JSON，确认格式和设置页一致；正常同步产生现有同步请求，暂停状态导入后没有重新上传。
- [x] 运行 `pnpm lint`、`pnpm exec tsc --noEmit`、聚焦 Prettier 和 `git diff --check`。
- [x] 运行 `pnpm build:offline` 并扫描离线 chunks，确认本地数据管理仍可用且不包含账号 API endpoint、账号面板“数据管理”入口或服务端模块；共享协调器中存在不带业务实现的 `account.data-manager` ID 可以接受。自托管生产构建的路由清单和构建产物中也不存在已删除的账号导出 route。
- [x] 关闭浏览器与开发服务，删除隔离数据库、WAL/SHM、上传目录、构建产物和临时验证文件；保留用户明确要求保留的截图。

验证期间仍可观察到共享 Modal 在父层被覆盖时将布尔 `inert` 传给 React 的既有控制台警告，以及项目既有 Sass `@import` 弃用警告；两者均不由本次数据管理改动引入。
