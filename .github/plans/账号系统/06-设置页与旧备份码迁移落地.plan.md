---
name: 设置页与旧备份码迁移落地
overview: 设置页账号管理、同步状态、旧备份码导入和匿名云备份入口下线步骤。
isProject: false
---

# 设置页与旧备份码迁移落地

## 一、目标

- 设置页保留本地导入导出。
- 未登录用户保留旧备份码导入入口，引导登录或注册后导入。
- 账号功能启用后，登录用户使用账号同步，不展示匿名云备份上传、还原、删除主入口。
- 账号功能禁用时，设置页保持当前环境原有可用能力；offline/static 只展示本地导入导出，legacy server 环境保留旧匿名云备份入口。
- 旧备份码导入成功后清除本地 `cloudCode`。

## 二、现有代码

- [app/(pages)/preferences/dataManager.tsx](<../../../app/(pages)/preferences/dataManager.tsx>) 当前包含本地导入导出、匿名云备份上传、还原、删除。
- [app/api/v1/backups/route.ts](../../../app/api/v1/backups/route.ts) 是旧匿名云备份上传。
- [app/api/v1/backups/[code]/route.ts](../../../app/api/v1/backups/[code]/route.ts) 是旧备份码下载和删除。
- [app/api/v1/backups/[code]/metadata/route.ts](../../../app/api/v1/backups/[code]/metadata/route.ts) 是旧备份码元数据查询。
- [app/api/v1/backups/cleanup/[secret]/route.ts](../../../app/api/v1/backups/cleanup/[secret]/route.ts) 是旧备份清理入口。
- [app/actions/backup/compatibility.ts](../../../app/actions/backup/compatibility.ts) 兼容旧稀客-only 数据。
- [app/actions/backup/db.ts](../../../app/actions/backup/db.ts) 和 [app/actions/backup/file.ts](../../../app/actions/backup/file.ts) 可复用到导入。

## 三、新增文件

| 文件                                             | 作用                               |
| ------------------------------------------------ | ---------------------------------- |
| `app/(pages)/preferences/accountManager.tsx`     | 登录状态、登录/注册/退出、账号操作 |
| `app/(pages)/preferences/accountSyncStatus.tsx`  | 最近同步、待上传、失败、冲突入口   |
| `app/(pages)/preferences/legacyBackupImport.tsx` | 输入旧备份码并导入到账号           |
| `app/components/accountConflictModal.tsx`        | 全局冲突选择窗口，设置页只触发打开 |
| `app/api/v1/sync/import-backup-code/route.ts`    | 旧备份码一次性导入 API             |

## 四、设置页状态

| 状态           | 展示                                                        |
| -------------- | ----------------------------------------------------------- |
| 账号功能禁用   | 保持当前环境原有可用能力；offline/static 只展示本地导入导出 |
| 未登录         | 本地导入导出、登录/注册入口、旧备份码导入入口               |
| 已登录         | 本地导入导出、账号同步状态、退出、清空云数据、逻辑删除账号  |
| 已登录且有失败 | 同步失败状态、重试入口                                      |
| 已登录且有冲突 | 待处理冲突入口、冲突窗口                                    |

## 五、旧备份码导入 API

`POST /api/v1/sync/import-backup-code` 步骤：

1. 校验普通 session。
2. 对 code 执行 `trim()` 后校验 UUID 格式，客户端自动导入和服务端 API 使用同一个规范化 code。
3. 使用规范化 code 定位旧备份；legacy 上传和账号导入同一 code 时通过进程内 code lock 串行执行。
4. 在 SQLite transaction 中 claim `backup_files` 记录后读取 `upload/backups/{code}.json`；文件读取或校验失败时回滚 transaction，不消费 code。
5. 读取前检查备份 JSON 文件大小，沿用 legacy 匿名上传 10MB UTF-8 byte 上限；超过上限返回稳定 `invalid-backup-file`。
6. 兼容旧稀客-only 数据和 `{ customer_normal, customer_rare }` 数据。
7. 对导入套餐先执行 `deleteIndexProperty` 清洗，再执行旧稀客兼容、validate、按 namespace 白名单规范化、merge。
8. 旧稀客-only 数据映射到 `customer_rare.meals`。
9. `{ customer_normal, customer_rare }` 分别映射到 `customer_normal.meals` 和 `customer_rare.meals`。
10. 账号已有云端套餐时，使用 [05-namespace序列化与冲突合并落地.plan.md](05-namespace序列化与冲突合并落地.plan.md) 的套餐合并规则，不覆盖整份云端数据。
11. 每个被写入 namespace 的 revision 加 1。
12. 旧备份码导入不递增 `state_epoch`。
13. 在 SQLite transaction 中先校验当前 `users.state_epoch`；不匹配时返回 `state-epoch-mismatch`，不消费 code。
14. 在同一个 transaction 中原子 claim：`DELETE backup_files WHERE code = ? RETURNING *`；claim 失败时不写入 `user_state`。
15. claim 成功后在同一个 transaction 中读取、校验并规范化 legacy JSON，然后按当前 namespace revision 执行条件 insert/update；并发普通同步已推进 revision 时回滚并返回 `sync-conflict`，不消费 code。
16. transaction 成功后删除 JSON 文件。
17. 文件删除失败时记录为孤儿文件，不恢复 `backup_files`；cleanup 会扫描无 DB record 的孤儿 JSON 文件并尝试清理。
18. 返回每个被导入 namespace 的 `namespace`、`revision`、`status`。
19. 客户端收到成功响应后立即 `GET /api/v1/sync/state` 校准 revision，再执行本地数据接管。

## 六、旧入口下线

- 未登录用户不再展示“备份至云端”。
- 未登录用户输入旧备份码时，引导登录或注册。
- 登录用户不再展示匿名云备份上传、还原、删除按钮。
- `globalStore.persistence.cloudCode` 只用于显示旧备份码导入提示。
- 导入成功后执行 `globalStore.persistence.cloudCode.set(null)`。
- 导入失败时保留本地 `cloudCode`，设置页继续展示重试入口。

## 七、本地导入导出保留

- 本地导出继续使用 `downloadJson`。
- 本地导入继续使用 `parseJsonFromInput`。
- 本地导入后，如果用户已登录，则导入结果进入 dirty queue，由账号同步上传。
- 本地重置后，如果用户已登录，则写入空套餐快照并同步。

## 八、验证点

- 未登录仍可本地导入导出。
- 登录用户看不到匿名云备份上传、还原、删除主入口。
- 本地已有 `cloudCode` 时首次引导展示旧备份码导入提示。
- 同一个 code 并发导入只有一个成功。
- code 前后带空白时，手动导入和登录后自动导入都按 trim 后 UUID 处理。
- 导入成功后 `cloudCode` 清空，旧 code 再次导入失败。
- legacy CORS 回归只覆盖 backups 根路由、`[code]` 和 `metadata`；cleanup secret 路由不开放 wildcard CORS。
- 并发导入同一个 code 时，失败请求不会写入 `user_state`。
- 旧备份上传和账号导入都按 UTF-8 byte 计算 10MB 上限。
- cleanup 返回删除/失败计数而不返回完整 code 列表；未下载过的备份按 `created_at` 判断过期，已下载过的备份按 `last_accessed` 判断过期。过期 DB 记录即使文件已缺失也会清理记录，并会清理 `upload/backups/*.json` 中已无 DB 记录的孤儿文件；cleanup 删除前会获取同 code 锁并重新确认 DB 状态，避免和正在进行的同 code 上传/导入交叉。
- legacy 覆盖上传已有 code 时会重置 `last_accessed = -1`，让新内容按新的 `created_at` 重新计算过期时间。

## 九、代码落地状态

- 已新增 [app/api/v1/sync/import-backup-code/route.ts](../../../app/api/v1/sync/import-backup-code/route.ts)。
- 导入 API 已实现普通 session、same-origin、CSRF、trim 后 UUID code 校验、旧稀客-only 兼容、`deleteIndexProperty` 清洗、同 code 进程内串行锁、transaction 内 `state_epoch` 校验、`DELETE backup_files ... RETURNING` claim、旧文件读取与校验、revision 条件写入 `user_state` 和成功后的 JSON 文件删除。
- 已新增 [app/(pages)/preferences/accountManager.tsx](<../../../app/(pages)/preferences/accountManager.tsx>)、[app/(pages)/preferences/accountSyncStatus.tsx](<../../../app/(pages)/preferences/accountSyncStatus.tsx>) 和 [app/(pages)/preferences/legacyBackupImport.tsx](<../../../app/(pages)/preferences/legacyBackupImport.tsx>)，接入登录、注册、退出、清空云端数据、删除账号、同步状态、失败重试和旧备份码导入。
- 登录、注册或已有 session bootstrap 后会先尝试导入本地 `cloudCode`，成功后清空本地旧备份码；导入失败时保留本地旧备份码并继续执行本地数据接管，未登录时也可在设置页录入旧备份码等待登录后导入。
- 登录、注册、已有 session bootstrap 或设置页手动导入期间暂停 watcher 产生的 dirty 入队；导入和本地接管所需的显式 dirty entry 正常写入，流程完成后恢复同步调度。
- 已更新 [app/(pages)/preferences/content.tsx](<../../../app/(pages)/preferences/content.tsx>)，将账号管理区域接入设置页。
- 已更新 [app/(pages)/preferences/dataManager.tsx](<../../../app/(pages)/preferences/dataManager.tsx>)，账号功能启用时隐藏旧匿名云备份主入口；账号功能禁用且仍为 self-hosted server 环境时保留 legacy 云备份入口，offline、Vercel/static export 只展示本地导入导出。
