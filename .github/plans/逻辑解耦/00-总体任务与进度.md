# 逻辑解耦改造总控

## 当前状态

- 总体状态：阶段 0 ~ 5 主路径已完成；阶段 6 进行中（非主路径）
- 当前阶段：阶段 6（非主路径）
- 当前焦点：阶段 6（非主路径）
- 当前优先级：阶段 6（低优）
- 当前里程碑：PR-6.5 已按最小切口完成；阶段 6 的剩余低收益候选已复核并继续跳过
- 最后更新：2026-04-28
- 当前基线验证：`pnpm exec tsc --noEmit` 通过；`pnpm lint` 0 error / 10 warnings；`pnpm build` 通过，静态页 `137/137`

## 不可偏移的约束

- `@davstack/store` 当前采用 `.computed(...).actions(...)` 链式声明；action 可以消费先声明的 computed，computed 不能依赖后声明的 action。
- `customer-rare` / `customer-normal` 的 `[[...paths]]/content.tsx` 仍承接路径同步、Tabs 壳与右侧卡片组合；route 级顾客列表、食材列表与标题副作用已分别收口到业务 hook。
- `savedMealCard` 的重排逻辑依赖“可见顺序 -> 原始数据索引”映射；后续 view-model 设计必须保留 `dataIndex` / `visibleIndex`。
- 稀客 / 普客并非完全同构；`bondRewards`、`suggestedMealCard`、复杂 selection tip、dark ingredient policy 都不能按对称假设硬合并。
- 已有共享基础必须复用：`InfoButtonBase`、`Plus`、`UnknownItem`、`MoveButton`、`TagGroup`、共享 ingredient props type。

## 阶段总表

| 阶段   | 目标                               | 主要输出                                                                                                           | 前置                  | 状态   | 备注                                                         |
| ------ | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------ | --------------------- | ------ | ------------------------------------------------------------ |
| 阶段 0 | 纯函数下沉                         | `buildRecipeSuitabilityRows`、`buildBeverageSuitabilityRows`、`getIngredientScoreChanges` 等                       | 无                    | 已完成 | `PR-0.1 ~ PR-0.6` 已落地，尚未切换旧调用点                   |
| 阶段 1 | 建立稳定的 state / view-model 载体 | store computed、route business hooks、推荐卡片 hook                                                                | 阶段 0 核心纯函数完成 | 已完成 | `PR-1.1 ~ PR-1.5` 已落地                                     |
| 阶段 2 | 视图组件削薄                       | tab content、saved meal、result/customer/info、route content 改读新 view-model                                     | 阶段 1 关键节点完成   | 已完成 | PR-2.1 ~ PR-2.7 已完成或判定无需空改动                       |
| 阶段 3 | 共享子块整合                       | 共享 tab body、shared row / list / avatar 片段                                                                     | 阶段 2 稳定           | 已完成 | PR-3.1 ~ PR-3.7 已完成                                       |
| 阶段 4 | 领域单例升级                       | `Recipe` / `CustomerRare` / `CustomerNormal` 中层聚合方法                                                          | 阶段 0-2 经验沉淀     | 已完成 | PR-4.1 ~ PR-4.4 已完成                                       |
| 阶段 5 | 清理与防退化                       | 死代码删除、缓存替换、repo memory 更新                                                                             | 前面阶段稳定          | 已完成 | PR-5.1 ~ PR-5.3 已完成                                       |
| 阶段 6 | 阶段 5 之后的补遗                  | store factory、Tab 视图共享化、resultCard/savedMealCard/customerCard 小共享、`useCustomerRouteData` 提升、低优收尾 | 未开始（非主路径）    | 进行中 | PR-6.1a、PR-6.2、PR-6.3、PR-6.4、PR-6.5 已完成；PR-6.1b 跳过 |

## 文档规划

| 文件                                                           | 作用        | 对应范围                                                   | 状态     |
| -------------------------------------------------------------- | ----------- | ---------------------------------------------------------- | -------- |
| [README.md](README.md)                                         | 总纲        | 最终方案、分阶段蓝图                                       | 未开始   |
| [00-总体任务与进度.md](00-总体任务与进度.md)                   | 总控        | 总任务、步骤、进度、风险锚点                               | 当前文件 |
| [01-阶段0-纯函数下沉.md](01-阶段0-纯函数下沉.md)               | 阶段 0 细化 | PR-0.1 ~ PR-0.6、输入输出、调用点、落地顺序                | 进行中   |
| [02-阶段1-state与view-model.md](02-阶段1-state与view-model.md) | 阶段 1 细化 | store computed、business hooks、推荐卡片 view-model        | 进行中   |
| [03-阶段2-视图削薄.md](03-阶段2-视图削薄.md)                   | 阶段 2 细化 | 各页面组件切换策略、依赖切换与回归点                       | 已完成   |
| [04-阶段3-共享整合.md](04-阶段3-共享整合.md)                   | 阶段 3 细化 | 共享子块、shared surface 与禁止合并边界                    | 已完成   |
| [05-阶段4-领域升级.md](05-阶段4-领域升级.md)                   | 阶段 4 细化 | 领域收口、单例方法边界与禁止上提清单                       | 已完成   |
| [06-阶段5-清理与防退化.md](06-阶段5-清理与防退化.md)           | 阶段 5 细化 | ESLint、缓存、死代码、repo memory 与最终验证               | 已完成   |
| [07-阶段6-补遗.md](07-阶段6-补遗.md)                           | 阶段 6 细化 | store factory、Tab 视图共享化、小共享、提升 hook、低优收尾 | 进行中   |
| [08-实施风格与约定.md](08-实施风格与约定.md)                   | 实施参考    | 导入边界、命名、组件 / hook / store / utils 风格与例外     | 已建立   |

## 当前步骤

1. 阶段 0 已完成：`app/utils/customer/shared/` 已落地 `PR-0.1 ~ PR-0.6` 的纯函数与 shared barrel，且保持旧页面/store 调用点不变。
2. 阶段 1 已完成：`customer-rare` / `customer-normal` store 已承接表格 rows、`ingredientScoreChanges` 与 `savedCustomerMealsWithEvaluation`，稀客侧已补齐 `currentMealPrice`、`unsatisfiedSelectionTip`、`bondRewards`、`useSuggestedMealsViewModel`，route content 也已改读 `useCustomerRouteData`、`useIngredientRouteData` 与 `useDocumentTitle`。
3. 阶段 2 已完成：PR-2.1 ~ PR-2.3、PR-2.5 ~ PR-2.7 当前仓库已满足首版目标，PR-2.4 完成了 rare `suggestedMealCard` 的业务输入拼装下沉。
4. 阶段 3 已启动：PR-3.1 已将 `InfoButtonBase`、`TagGroup`、`Plus`、`UnknownItem`、`MoveButton`、tab 常量与基础类型 surface 收口到 `app/(pages)/customer-shared/`，并清除了 `customer-normal` 对 `customer-rare` 的页面级横向 import。
5. PR-3.2 已完成：新增 `app/(pages)/customer-shared/beverageTableShell.tsx` 与 `app/(pages)/customer-shared/recipeTableShell.tsx`，并让 rare / normal 的 `beverageTabContent.tsx`、`recipeTabContent.tsx` 共用 table shell；toolbar、排序分页计算、`selectedKeys` 与 `renderTableCell` 继续保留在页面文件。
6. PR-3.3 已完成：新增 `app/(pages)/customer-shared/currentMealIngredientsList.tsx`，并让 rare / normal 的 `resultCard.tsx` 共用当前套餐食材槽位列表；原始食材推导、删除动作、价格、保存按钮与厨具逻辑仍保留在页面文件。
7. PR-3.4 已完成：新增 `app/(pages)/customer-shared/savedMealIngredientsStrip.tsx` 与 `app/(pages)/customer-shared/savedMealActionRail.tsx`，并让 rare / normal 的 `savedMealCard.tsx` 共用 saved meal 条带与动作轨道；visible meals 计算、`dataIndex` / `visibleIndex` 映射、evaluation 与 rare 选择恢复逻辑仍保留在页面文件。
8. PR-3.5 已完成：新增 `app/(pages)/customer-shared/ingredientTabContentSkeleton.tsx` 与 `app/(pages)/customer-shared/ingredientTabItemPresenter.tsx`，并让 rare / normal 的 `ingredientTabContent.tsx` 共用 ingredient tab 的骨架与单格 presenter；dark ingredient policy、local filter 规则与 rare 的 Dark Matter / order tag 语义仍保留在页面文件。
9. PR-3.6 已完成：新增 `app/(pages)/customer-shared/ratingAvatarShell.tsx`，并让 rare / normal 的 `customerCard.tsx` 与 `savedMealCard.tsx` 共用 rating avatar overlay 壳；avatar 本体、fallback、无评级文案与 rare / normal 本地 rating 语义仍保留在页面文件。
10. PR-3.7 已完成：在 `app/(pages)/customer-shared/infoButtonBase.tsx` 上补了 `InfoSectionTitle`，并让 rare / normal 的 description section 共用这一中性标题壳；`defaultExpandedKeys`、奖励树、符卡树、chat、rating legend、help 与 tachie / jump 交互继续保留在页面文件。
11. PR-4.1 已完成：`Recipe.buildRecipeSuitabilityRows` 已承接原始 recipe suitability rows 构造，shared helper 继续保留 hidden/search/filter/sort/paginate 的薄包装职责，rare / normal store 的 helper 入参面也已收缩到 `recipeInstance`、`popularTrend`、`isFamousShop` 与表格状态。
12. PR-4.2 已完成：`CustomerRare.getDisplayMeta` 已承接 rare `customerCard` 当前稳定的 profile meta，`customerCard.tsx` 改为消费领域方法；rating UI、tooltip / popover JSX、tag tooltip、默认展开项与 infoButton 相关派生仍保留在页面层。
13. PR-4.3 已完成：`CustomerNormal.getDisplayMeta` 已承接 normal `customerCard` 当前稳定的地点类 profile meta，`customerCard.tsx` 改为消费领域方法；`地精` 的地点展示特例、rating UI、tooltip / popover JSX 与筛选语义仍保留在页面层。
14. PR-4.4 已完成：`Beverage.buildBeverageSuitabilityRows` 已承接原始 beverage suitability rows 构造，shared helper 继续保留 hidden/search/filter/sort/paginate 的薄包装职责，rare / normal store 的 helper 入参面也已收缩到 `beverageInstance` 与表格状态。
15. PR-5.1 已完成：删除了 `app/(pages)/customer-rare/ingredientTabContent.tsx` 中已被 `ingredientScoreChanges` 承接面的页面层 `darkIngredient` fallback；其余候选文件当前剩余逻辑经盘点后确认仍承担 UI 配置、路由壳或展示差异职责，不做无收益硬删。
16. PR-5.2 已完成：`app/utils/customer/shared/evaluateSavedMeals.ts` 的 live runtime 保存套餐评估链已切到容量 `256` 的 simple LRU bounded cache；rare / normal store 中遗留的 `evaluateSavedMealResult` action 与 store-local `savedMealRatingCache` 已清理，继续沿用 shared helper 内的 `JSON.stringify(data)` key 且不改返回结构分叉。
17. PR-5.3 已完成：阶段 5 文档、总控文件、阶段 6 文档、计划 README 与 repo memory 已回写到最终一致状态；阶段 5 至此收口完成。
18. PR-6.1a 已完成：新增 `app/stores/utils/applyTableSortChange.ts`，并把 rare / normal store 的四个表格排序 handler 切到共享三态排序状态机；本轮不触碰 `available*` computed。
19. PR-6.1b 已完成只读定界：13 个 `available*` computed 虽然是机械重复，但整包抽成 factory 会明显变宽；当前决定跳过该子切口，不单列实现。
20. PR-6.2 已完成最小安全切口：新增 `app/(pages)/customer-shared/customerTabShell.tsx`，并把 rare / normal 的 `customerTabContent.tsx` 收口为只保留 store、routePrefix、振动与 avatar 差异的薄 wrapper；beverage / recipe 的 toolbar + cell 当前明确跳过。
21. PR-6.3 已完成最小安全切口：新增 `app/hooks/useAutoHideTooltip.ts` 与 `app/(pages)/customer-shared/swapSavedMeals.ts`，并让 rare / normal 的 `resultCard.tsx`、`savedMealCard.tsx` 共享 tooltip 自动隐藏与保存套餐重排核心；整卡合并与 `CustomerDlcPill` 当前明确跳过。
22. PR-6.4 已完成最小安全切口：新增 `app/hooks/useCustomerRouteData.ts`，并把 rare / normal route content 的 customer 列表搜索、筛选、排序链切到 hooks 统一出口；两个 page-local `useCustomerRouteData.ts` 副本已删除，`currentBeverageTags` 与 `suggestMeals -> isDarkMatter` 当前明确跳过。
23. PR-6.5 已完成最小安全切口：已将 rare / normal `evaluateMeal.ts` 中的本地 `getRatingKey` 语义化重命名为 `getRareMealRatingKey` / `getNormalMealRatingKey`，并新增 `app/utils/customer/shared/getRestExtraIngredients.ts` 统一 shared 组件与 hook 中的可见额外食材裁剪逻辑；`useIngredientTabPrelude` 当前仍明确跳过。
24. 阶段 6 当前仍是非主路径、低优后置补遗；`available*` computed、`currentBeverageTags` store computed、`suggestMeals -> isDarkMatter` 与 `useIngredientTabPrelude` 已在 latest code 下复核，结论均为继续跳过。
25. 本次分支复审核对出的已知 findings 已按最小切口收口；若后续再重访低收益候选，应先有新的消费者或新的稳定输入面，而不是为了共享率重开既有低收益候选。
