---
name: 可获取性领域目录与全局可见性接入
overview: 将已复核的 availabilityPaths 固化为单例领域目录，为十类资料实例附加派生字段，并将全局“拥有的游戏内容”语义接入所有资料可见性读取。
todos:
    - id: catalog-api
      content: 建立稳定的领域目录、路径满足判断与单 DLC 筛选投影
      status: completed
    - id: instance-enrichment
      content: 为十类资料单例附加 availabilityPaths 和 availabilityDlcs
      status: completed
    - id: global-visibility
      content: 用共享判断替换按 item.dlc 裁剪的全局可见性
      status: completed
    - id: ownership-copy
      content: 明确全局数据集开关表示玩家拥有的游戏内容
      status: completed
isProject: false
---

# 可获取性领域目录与全局可见性接入计划

> **执行要求：** 每个行为改动先在可重复执行的 `tsx` 断言中制造预期失败，再写最小实现并重跑静态检查。本仓库没有自动化测试套件，不虚构测试或覆盖率结果。

**目标：** 让每条应用层资料都携带同一份可获取路径，并让全局 DLC/模组开关按“玩家是否拥有路径所需内容”判断可见性。

**架构：** `app/domain/availability/catalog.ts` 在模块生命周期内只派生一次全量目录，以 `category:id` 索引稳定结果。`Item` 基类构造时根据明确的资料类别附加 `availabilityPaths` 和单 DLC 筛选投影 `availabilityDlcs`；所有可见性消费方调用同一判断器，不再各自比较 `item.dlc`。

**技术栈：** strict TypeScript、`@davstack/store`、现有 Item/Food/Customer 单例和 `tsx` 断言；不增加依赖。

## 全局约束

- `app/domain/data` 中的 `dlc` 仍是内容归属，不因可获取路径而改写。
- 派生目录是只读运行时数据，不放入持久化、账号同步或跨标签广播。
- 本体 DLC0 始终满足；其他条件以现有 `hiddenItems.dlcs` 存储的反集判断。
- 路径内 `requiredDlcs` 为 AND，路径之间为 OR。
- 单 DLC 的“可获取于”投影只接受 `[0]` 或只有一个非零条件的路径；`[1,4]` 不伪装成 DLC1 或 DLC4 的单选结果。
- 保留现有持久化字段形状和同步集合合并策略，仅更新产品文案与下游语义。
- 不创建分支、worktree 或提交。

## 公开接口

```ts
export interface IAvailabilityItemData {
	availabilityDlcs: readonly TDlc[];
	availabilityPaths: readonly IAvailabilityPath[];
}

export function getAvailabilityEntry(
	category: TAvailabilityCategory,
	id: number
): IAvailabilityAuditEntry;

export function projectAvailabilityDlcs(
	paths: ReadonlyArray<IAvailabilityPath>
): TDlc[];

export function isAvailableWithHiddenDlcs(
	paths: ReadonlyArray<IAvailabilityPath>,
	hiddenDlcs: ReadonlySet<TDlc>
): boolean;
```

## 任务一：目录与共享判断

**文件：**

- 新建 `app/domain/availability/catalog.ts`。
- 修改 `app/domain/availability/types.ts` 和 `app/domain/availability/index.ts`。
- 修改 `scripts/verifyAvailabilityPaths.ts`。

- [x] 断言奶油的投影为 `[1,3,4,5]`，魔法森林钓鱼的 `[1,4]` 组合路径本身不投影成单 DLC，本体路径投影为 `[0]`。
- [x] 断言仅拥有本体和 DLC4 时奶油可见，DLC1 专属顾客不可见，魔理沙仍可见。
- [x] 实现一次性目录索引、重复键/缺失键失败和路径满足判断。
- [x] 重跑断言和 `pnpm exec tsc --noEmit`。

## 任务二：十类单例附加字段

**文件：**

- 修改 `app/domain/catalog/shared/Item.ts` 和 `app/domain/catalog/shared/types.ts`。
- 修改 `app/domain/catalog/food/Ingredient.ts`、`Beverage.ts`、`Recipe.ts`。
- 修改 `app/domain/catalog/items/Clothes.ts`、`Cooker.ts`、`Currency.ts`、`Ornament.ts`、`Partner.ts`。
- 修改 `app/domain/catalog/customers/CustomerNormal.ts` 和 `CustomerRare.ts`。

- [x] 断言十类资料的每条派生结果都携带非空 `availabilityPaths`，稳定键与原始 ID 一一对应。
- [x] 让 `Item` 构造器接收 `TAvailabilityCategory`，先附加只读领域字段，再生成现有拼音字段。
- [x] 各单例显式传入自身类别，不根据 ID 区间、名称或运行时字段猜测类别。
- [x] 确认原有拼音排序、名称索引和弱引用缓存仍可读取新字段。

## 任务三：全局可见性与设置语义

**文件：**

- 修改 `app/features/catalog/shared/client/hooks/useFilteredData.ts`、`app/features/catalog/customers/shared/client/hooks/useCustomerRouteData.ts` 和所有按 `hiddenDlcs` 裁剪资料的 feature store/领域查询。
- 修改 `app/features/preferences/client/components/PreferencesContent.tsx`、`HiddenItems.tsx`、`app/features/account/client/components/accountConflict/AccountConflictModal.tsx` 和相关引导文案。
- 保持 `app/features/preferences/client/state/globalPersistenceStore.ts`、`app/features/account/sync/serializers/globalPreferences.ts`、`globalPreferencesMerge.ts` 与 `validation.ts` 的存储形状不变。

- [x] 用共享 `availabilityPaths` 判断替换普通 `!hiddenDlcs.has(item.dlc)`；删除魔理沙的平行硬编码可见集合。
- [x] 确认所有 store 候选值先按当前拥有内容裁剪，再提取地区、标签、类型和名称。
- [x] 数据集开关选中表示拥有，DLC2.5 与其他 DLC 使用同样标签，MetaMystia 沿用模组标签，本体始终拥有。
- [x] 保持本地持久化、账号序列化、三方合并和双标签广播的现有字符串集合语义；浏览器已验证同时可见标签页同步。

## 历史阶段验收证据

- 当时运行 `pnpm exec tsx scripts/verifyAvailabilityPaths.ts`；该临时脚本已在生产接入与最终复审后删除，不是当前可执行命令。
- `pnpm exec tsc --noEmit`。
- `pnpm lint`。
- `pnpm exec prettier --check <本阶段文件>`。
- `git diff HEAD --check`。
